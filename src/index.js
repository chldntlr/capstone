import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';  // 글로벌 CSS 파일을 import
import App from './App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
